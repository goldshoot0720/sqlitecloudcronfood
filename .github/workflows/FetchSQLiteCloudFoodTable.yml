name: Fetch SQLiteCloud Food Table

on:
  workflow_dispatch:   # 可手動觸發
  schedule:            # 每天凌晨 1 點自動執行
    - cron: "0 1 * * *"

jobs:
  fetch-food:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
